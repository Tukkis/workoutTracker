import React from 'react';
import { Header, Icon, Input, Button, ListItem } from '@rneui/themed';

export default function Calendar({ navigation }){
    return(
        <Header centerComponent={{ text: 'Excercises'}} />
    )
}